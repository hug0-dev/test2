{% extends 'base.html.twig' %}

{% block title %}Modifier un Ouvrier{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Modifier un Ouvrier</h1>

        <div class="card">
            <div class="card-body">
                {{ include('ouvrier/_form.html.twig') }}
            </div>
        </div>

        <div class="mt-3">
            <a href="{{ path('app_ouvrier_index') }}" class="btn btn-secondary">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
